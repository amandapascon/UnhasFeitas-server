const mongoose = require('mongoose')

const Scheduling = mongoose.model('Scheduling')

module.exports = {
    //new scheduling

    //user scheduling

    //delete scheduling

    //show all scheduling (admin)
}